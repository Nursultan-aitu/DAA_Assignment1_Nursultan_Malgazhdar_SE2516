# DAA Assignment 1: Sorting Algorithms Analysis

## How to run tests
Execute the following command in the terminal to run all JUnit tests:
`mvn test`

## How to run benchmarks
Run the `main` method in the `kz.aitu.daa.assignment1.BenchmarkRunner` class from your IDE. The results will be automatically saved to `results.csv`.